apiVersion: v1
kind: PersistentVolume
metadata:
 name: tekton-app-pv
spec:
 capacity:
  storage: 1Gi
 accessModes:
  - ReadWriteMany
 persistentVolumeReclaimPolicy: Recycle
 nfs:
  server: 10.0.1.76 # private ip for common-control-plane-node
  path: /tekton/app
